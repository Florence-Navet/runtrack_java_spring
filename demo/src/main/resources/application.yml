spring:
  application:
    name: demo
  profiles:
    active: dev

---
spring:
  config:
    activate:
      on-profile: dev
greeting:
  message: "Hello World from DEV"
  tools: "Hello Tools depuis DevTools"
---
spring:
  config:
    activate:
      on-profile: prod
greeting:
  message: "Hello World from PROD"
  tools: "Hello Tools depuis PROD"